-- ======================================================================
-- Wave 4.6 - 评估中心 加 p-value 列
-- ======================================================================
-- 目的：A/B 评估完成后做配对 t 检验，判断差异在统计意义上是否显著。
-- 范围：[0, 1]，DECIMAL(6,4) 即 0.9999（4 位精度足够，统计学惯例）。
-- ======================================================================

ALTER TABLE `eval_task`
  ADD COLUMN `p_value` DECIMAL(6,4) NULL COMMENT 'paired t-test p-value (Wave 4.6)';
