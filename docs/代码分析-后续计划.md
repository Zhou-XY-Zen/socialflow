# 代码分析模块 · Wave 路线图

> 当前 Wave 6 进行中。本文档随每个 Wave 完成同步更新。

---

## ✅ 已完成

### Wave 5 — 基础架构
- 三种分析模式（项目概览 / 提交审查 / 对比分析）
- 8 个前端页面（仪表盘/项目概览/提交审查/对比/历史/收藏/凭证/规约库/设置）
- V7 表（repo_analysis / repo_analysis_finding / repo_bookmark）
- V8-V11 仓库凭证（Token + 账号密码 + 父子凭证项目）
- 规约库静态前端 236 条（早期估测版）

### Wave 5.5 — 准确性 + 可观测
- 审查链路全量重写为 Map-Reduce：项目概览按模块、审查按文件，告别截断
- V12 `llm_call_log` 表 + 三处展示（Dashboard Token 卡 / History Token 列 / AnalysisResult LLM 折叠面板）
- 黄山版 1.7.1 PDF 真实抽取 321 条规约（覆盖前端 rules-data.ts + service resources/rules SOT JSON）
- AsyncConfig 自定义线程池（codeAnalysisExecutor / knowledgeIngestExecutor）

### Wave 5.6 — 质量与安全治理
- A-F 误判防护 6 层：codeSnippet 反向校验 + ruleRef 白名单 + lineRange 行内容校验 + diff ±5 行上下文 + LLM self-check + PMD/P3C profile
- V13 拓宽 finding 字段（rule_ref 128→255 等）
- V14 `llm_call_log` 补 update_time + is_deleted
- 配置安全：application.yml 删默认密码 + 服务器 .env 注入 + redeploy.py 启动时 source
- 第二轮真问题清理：STATUS_* 常量化 + Lombok @Data POJO + catch 升级日志 + 前端 Token 阈值常量化

---

## 🚀 进行中

### Wave 6 — 智能化（LLM × 规约真正闭环）
**目标**：finding 的每条 ruleRef 都引用真实可点击的 X.Y.Z + 引入静态扫描双引擎

**任务清单**：
- [x] **C-1** LLM Prompt 注入按文件类型挑选的相关规约（基础设施 `RuleLibraryHolder.pickForFile` 已就绪）
- [x] ~~**F-1** 跑通 `mvn -P p3c verify`，评估 PMD 报告 vs LLM 的重叠 / 互补~~
   - **结论：暂搁置**。PMD 6.55 + p3c-pmd 2.1.1 都基于 ASM 旧版本，不支持 JDK 21 的 class file major version 65，所有文件解析失败 → 0 violations。
   - PMD 7 不兼容 p3c-pmd 2.1.1 API，需要等阿里更新或社区 fork。
   - 替代方案候选：SpotBugs（字节码导向，跟得上新 JDK）/ Checkstyle（纯源码扫描）/ 用 IDEA P3C 插件做开发期检查。
   - 决策：当前阶段不引入新静态扫描工具，专注 LLM 审查 + Wave 6 其他子任务。
- [ ] 历史误判 finding 批量 IGNORED 清理（清掉之前 18+14 条里的误判残留）
- [ ] 重跑提交审查 + 出对比报告

**验收标准**：
- HIGH = 0
- 整体评分 ≥ 75
- 抽样 finding 中 ruleRef 全为真实存在的 X.Y.Z 编号
- 误判率 < 5%

---

## 📋 待启动

### Wave 7 — 可管理性（规约库持久化 + UI 可控）
**目标**：把内存 holder 的 321 条规约持久化到表，可启停 / 可自定义 / 可升级

**任务清单**：
- V15 migration `rule_library` 表
- `RuleLibrary` entity / VO / Mapper / Service / Controller
- `RuleLibraryInitRunner` 启动 upsert from JSON（首次 / 升级时同步）
- `RuleLibraryHolder` 改为从 DB 加载，启动 JSON 兜底
- Rules.vue 改 API 加载 + 启停按钮 + 自定义规约新增
- `rules-data.ts` 188KB 静态数据可移除（前端瘦身）

**触发条件**：用户提出"想关掉某条规约"或"想加自己团队的规约"时启动

---

### Wave 8 — 反馈闭环（用户标记驱动 Prompt 改进）
**目标**：用户标"无效 finding" → 系统记录 → 长期降低误判

**任务清单**：
- V16 migration: finding 加 `dismissed_reason` 字段（"误判 / 已修复 / 不适用 / 其他"）
- `updateFindingStatus` API 接受 reason
- AnalysisResult 详情面板加分类标记按钮
- 后台聚合：某 ruleRef 标无效次数 > N 时自动加入 prompt 屏蔽列表
- Dashboard 新增"误判率"卡 + Top 5 误报规约榜

**触发条件**：Wave 6 + Wave 7 上线 1-2 周积累真实数据后

---

### Wave 9 — 工程化与扩展（CI / 多仓库 / 多模型）
**目标**：从"用户手动触发"扩展到"自动化触发 + 长期跟踪"

**子任务**（按需独立做）：
- **9.1 Webhook 触发**：GitHub PR 推送自动审查 + 评论回写
- **9.2 批量定时扫描**：cron + 多仓库列表 + 失败邮件告警
- **9.3 仓库健康度时间线**：同仓库多次评分折线 + 趋势预警
- **9.4 多 LLM provider 评估**：DeepSeek vs Qwen vs GLM 同样代码哪个更准（A/B 自动对比）

**触发条件**：有具体场景需求时按子任务启动

---

## 📅 时间表参考

```
本周：    Wave 6  （3-4 小时一气呵成）
下周：    Wave 7  （半天到一天）
观察期：  Wave 8 （收集生产 1-2 周真实反馈后启动）
按需：    Wave 9 子项
```

## 📌 长期想法池（暂不规划）

- Finding 类目热力图：按 category × 仓库矩阵展示违反密度
- 规约违反"修复时间"指标
- 团队成员代码质量对比看板
- 通过 LLM 自动生成 Pull Request 修复建议补丁
